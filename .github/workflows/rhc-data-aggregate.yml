name: RHC aggregate all wallet and NFT evidence

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - '.github/workflows/rhc-data-aggregate.yml'
      - 'rhc-data-aggregate/**'

permissions:
  actions: read
  contents: write

concurrency:
  group: rhc-data-aggregate-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  aggregate:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Wait for every source run, download, validate and merge
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python rhc-data-aggregate/aggregate.py
      - name: Upload complete immutable aggregate
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rhc-data-aggregate
          path: rhc-data-aggregate-output/
          retention-days: 10
          if-no-files-found: error
          compression-level: 9
      - name: Commit aggregate status and source run IDs to research branch
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add rhc-data-stage/
          if git diff --cached --quiet; then
            echo 'No aggregate status changes to commit.'
          else
            git commit -m 'data: record RHC aggregate status [skip ci]'
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi
