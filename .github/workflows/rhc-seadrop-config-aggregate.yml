name: RHC aggregate complete SeaDrop configuration history

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - '.github/workflows/rhc-seadrop-config-aggregate.yml'
      - 'rhc-seadrop-config-aggregate/**'

permissions:
  actions: read
  contents: write

concurrency:
  group: rhc-seadrop-config-aggregate-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  aggregate:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Wait for all SeaDrop shards and merge
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python rhc-seadrop-config-aggregate/aggregate.py
      - name: Upload complete SeaDrop history
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rhc-seadrop-config-aggregate
          path: rhc-seadrop-config-aggregate-output/
          retention-days: 10
          if-no-files-found: error
          compression-level: 9
      - name: Commit immutable source run and status
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add rhc-seadrop-config-stage/
          if git diff --cached --quiet; then
            echo 'No status change.'
          else
            git commit -m 'data: record SeaDrop config aggregate status [skip ci]'
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi
