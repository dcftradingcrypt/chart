name: RHC selection-alpha cap-safe cursor histories

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - '.github/workflows/rhc-selection-alpha-cursor.yml'
      - 'rhc-selection-alpha/fetch_cursor.py'

permissions:
  contents: read

jobs:
  history:
    strategy:
      fail-fast: false
      matrix:
        target: [seadrop, seaport]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Fetch complete cap-safe event history
        run: >-
          python rhc-selection-alpha/fetch_cursor.py
          --target '${{ matrix.target }}'
          --out 'out-${{ matrix.target }}'
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rhc-selection-alpha-cursor-${{ matrix.target }}
          path: out-${{ matrix.target }}/
          retention-days: 2
          if-no-files-found: error
          compression-level: 9
